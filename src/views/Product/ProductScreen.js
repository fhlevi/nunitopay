import { Grid } from '@mui/material';

function ProductScreen () {
    return (  
        <div>
            <Grid xs={12} p={2} item>
                Product
            </Grid>
        </div>
    );
}

export default ProductScreen;